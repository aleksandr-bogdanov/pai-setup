Read ~/.claude/skills/Utilities/DeepLoop/SKILL.md, then read ~/.claude/skills/Utilities/DeepLoop/LoopEngine.md and ~/.claude/skills/Utilities/DeepLoop/ArbiterFramework.md, then read ~/.claude/skills/Utilities/DeepLoop/Workflows/Audit.md. Follow the Audit workflow exactly. Target: $ARGUMENTS (if no target specified, ask the user what to audit). Auto-decompose into domains, show the domain map, and begin the convergence loop after user confirms.