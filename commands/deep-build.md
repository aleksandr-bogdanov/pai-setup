Read ~/.claude/skills/Utilities/DeepLoop/SKILL.md, then read ~/.claude/skills/Utilities/DeepLoop/LoopEngine.md and ~/.claude/skills/Utilities/DeepLoop/ArbiterFramework.md, then read ~/.claude/skills/Utilities/DeepLoop/Workflows/Build.md. Follow the Build workflow exactly. Feature to implement: $ARGUMENTS (if no feature specified, ask the user what to build). Decompose into implementation domains, show the plan, and begin iterative implementation after user confirms.