Read ~/.claude/skills/Utilities/DeepLoop/SKILL.md, then read ~/.claude/skills/Utilities/DeepLoop/LoopEngine.md and ~/.claude/skills/Utilities/DeepLoop/ArbiterFramework.md, then read ~/.claude/skills/Utilities/DeepLoop/Workflows/Investigate.md. Follow the Investigate workflow exactly. Investigation axes: $ARGUMENTS (if no axes specified, use defaults: security, correctness, test-coverage, consistency). Auto-decompose into domains per axis, then launch parallel investigation loops. Save reports to docs/investigations/.